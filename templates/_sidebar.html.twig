<aside class="sidenav navbar navbar-vertical navbar-expand-xs border-radius-lg fixed-start ms-2 bg-white my-2" id="sidenav-main">
  <div class="sidenav-header">
    <i class="fas fa-times p-3 cursor-pointer text-dark opacity-5 position-absolute end-0 top-0 d-none d-xl-none" aria-hidden="true" id="iconSidenav"></i>
    <a class="navbar-brand px-4 py-3 m-0" href="{{ path('app_home') }}">
      <img src="{{ asset('material-dashboard-master/assets/img/logoOur.png') }}" class="navbar-brand-img" width="26" height="26" alt="main_logo">
      <span class="ms-1 text-sm text-dark">Your App</span>
    </a>
  </div>
  <hr class="horizontal dark mt-0 mb-2">
  <div class="collapse navbar-collapse w-auto" id="sidenav-collapse-main">
    <ul class="navbar-nav">
      {% set current_route = app.request.attributes.get('_route') %}

      <li class="nav-item">
        <a class="nav-link {{ current_route == 'app_welcome' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_welcome') }}">
          <i class="material-symbols-rounded opacity-5">dashboard</i>
          <span class="nav-link-text ms-1">Dashboard</span>
        </a>
      </li>

      {# Hide "Utilisateurs" for Artisan and Constructeur #}
      {% if app.user and 'ROLE_ARTISAN' not in app.user.roles and 'ROLE_CONSTRUCTEUR' not in app.user.roles %}
        <li class="nav-item">
          <a class="nav-link {{ current_route starts with 'app_tables' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_tables') }}">
            <i class="material-symbols-rounded opacity-5">table_view</i>
            <span class="nav-link-text ms-1">Utilisateurs</span>
          </a>
        </li>
      {% endif %}

      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_projet_' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_projet_index') }}">
          <i class="material-symbols-rounded opacity-5">construction</i>
          <span class="nav-link-text ms-1">Projets</span>
        </a>
      </li>
      <!-- Tâches Menu Item -->
      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_tache_' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_tache_index') }}">
          <i class="material-symbols-rounded opacity-5">assignment</i>
          <span class="nav-link-text ms-1">Tâches</span>
        </a>
      </li>
      <!-- Plannification Menu Item -->
      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_plannification_' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_plannification_index') }}">
          <i class="material-symbols-rounded opacity-5">schedule</i>
          <span class="nav-link-text ms-1">Plannification</span>
        </a>
      </li>

      <!-- Saved Plannifications Menu Item -->
      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_plannification_saved' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_plannification_saved') }}">
          <i class="material-symbols-rounded opacity-5">bookmark</i>
          <span class="nav-link-text ms-1">Saved Plannifications</span>
        </a>
      </li>

      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_chat' ? 'active bg-gradient-dark text-white' : 'text-dark' }}"
           href="{{ path('app_chat') }}">
          <i class="material-symbols-rounded opacity-5">chat</i>
          <span class="nav-link-text ms-1">AI Assistant</span>
        </a>
      </li>

      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_terrain_index' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_terrain_index') }}">
          <i class="material-symbols-rounded opacity-5">landscape</i>
          <span class="nav-link-text ms-1">Terrains</span>
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_etapeprojet_' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_etapeprojet_index') }}">
          <i class="material-symbols-rounded opacity-5">list_alt</i>
          <span class="nav-link-text ms-1">Étapes des projets</span>
        </a>
      </li>

      {# Hide "Abonnements" for Artisan only #}
      {% if app.user and 'ROLE_ARTISAN' not in app.user.roles %}
        <li class="nav-item">
          <a class="nav-link {{ current_route starts with 'app_abonnement_' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_abonnement_index') }}">
            <i class="material-symbols-rounded opacity-5">list_alt</i>
            <span class="nav-link-text ms-1">Abonnements</span>
          </a>
        </li>
      {% endif %}

      <li class="nav-item">
        <a class="nav-link {{ current_route starts with 'app_contrat_' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{path('app_contrat_index') }}">
          <i class="material-symbols-rounded opacity-5">list_alt</i>
          <span class="nav-link-text ms-1">Contrats</span>
        </a>
      </li>

      {# Hide Stock/Fournisseur/Article/Equipe for Artisan and Constructeur #}
      {% if app.user and 'ROLE_ARTISAN' not in app.user.roles and 'ROLE_CONSTRUCTEUR' not in app.user.roles %}
        <li class="nav-item">
          <a class="nav-link {{ current_route starts with 'app_stock_index' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_stock_index') }}">
            <i class="material-symbols-rounded opacity-5">table_view</i>
            <span class="nav-link-text ms-1">Stock</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ current_route starts with 'app_fournisseur_index' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_fournisseur_index') }}">
            <i class="material-symbols-rounded opacity-5">table_view</i>
            <span class="nav-link-text ms-1">Fournisseur</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ current_route starts with 'app_article_index' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_article_index') }}">
            <i class="material-symbols-rounded opacity-5">table_view</i>
            <span class="nav-link-text ms-1">Article</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link {{ current_route starts with 'app_equipe_index' ? 'active bg-gradient-dark text-white' : 'text-dark' }}" href="{{ path('app_equipe_index') }}">
            <i class="material-symbols-rounded opacity-5">table_view</i>
            <span class="nav-link-text ms-1">Equipe</span>
          </a>
        </li>
      {% endif %}
    </ul>
  </div>
  <div class="sidenav-footer position-absolute w-100 bottom-0">
    <div class="mx-3">
      <a class="btn btn-outline-dark mt-4 w-100" href="#" type="button">Documentation</a>
    </div>
  </div>
</aside>