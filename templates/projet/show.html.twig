{% extends 'base.html.twig' %}

{% block title %}Projet{% endblock %}

{% block body %}
    <h1>Projet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdProjet</th>
                <td>{{ projet.idProjet }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ projet.type }}</td>
            </tr>
            <tr>
                <th>Stylearch</th>
                <td>{{ projet.stylearch }}</td>
            </tr>
            <tr>
                <th>Budget</th>
                <td>{{ projet.budget }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ projet.etat }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ projet.datecreation ? projet.datecreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nomprojet</th>
                <td>{{ projet.nomprojet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_projet_index') }}">Retour à la liste des projets</a>

    <a href="{{ path('app_projet_edit', {'idProjet': projet.idProjet}) }}">edit</a>

    {{ include('projet/_delete_form.html.twig') }}
{% endblock %}
