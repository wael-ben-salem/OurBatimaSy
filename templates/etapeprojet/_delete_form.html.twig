<form method="post" action="{{ path('app_etapeprojet_delete', {'idEtapeprojet': etapeprojet.idEtapeprojet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etapeprojet.idEtapeprojet) }}">
    <button class="btn">Delete</button>
</form>
