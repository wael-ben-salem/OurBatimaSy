{% extends 'base.html.twig' %}

{% block title %}Contrat{% endblock %}

{% block body %}
    <h1>Contrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdContrat</th>
                <td>{{ contrat.idContrat }}</td>
            </tr>
            <tr>
                <th>TypeContrat</th>
                <td>{{ contrat.typeContrat }}</td>
            </tr>
            <tr>
                <th>DateSignature</th>
                <td>{{ contrat.dateSignature ? contrat.dateSignature|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ contrat.dateDebut ? contrat.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>SignatureElectronique</th>
                <td>{{ contrat.signatureElectronique }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ contrat.dateFin ? contrat.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>MontantTotal</th>
                <td>{{ contrat.montantTotal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contrat_index') }}">back to list</a>

    <a href="{{ path('app_contrat_edit', {'idContrat': contrat.idContrat}) }}">edit</a>

    {{ include('contrat/_delete_form.html.twig') }}
{% endblock %}
