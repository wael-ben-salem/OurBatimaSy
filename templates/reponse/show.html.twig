{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reponse.description }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ reponse.statut }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reponse.date ? reponse.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_index') }}">back to list</a>
    {# Edit and delete links removed #}
{% endblock %}
